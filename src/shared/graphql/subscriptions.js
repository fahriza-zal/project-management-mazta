/**
 * GraphQL subscriptions (placeholder, planned for v2).
 *
 * Example:
 *   import { gql } from '@apollo/client/core'
 *
 *   export const TASK_UPDATED = gql`
 *     subscription TaskUpdated($projectId: ID!) {
 *       taskUpdated(projectId: $projectId) { id status }
 *     }
 *   `
 */
export {}
